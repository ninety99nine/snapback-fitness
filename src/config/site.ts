/**
 * SnapBack Fitness – site-wide configuration
 *
 * Update these values to configure the site without touching component code.
 */

export const siteConfig = {
  /**
   * Patlo's WhatsApp number.
   * Format: country code + number, digits only. No +, spaces, or dashes.
   * Example: South Africa +27 82 123 4567 → "27821234567"
   */
  whatsappNumber: "27000000000",

  /**
   * Direct URL to the free postpartum guide PDF.
   * Can be a relative path ("/snapback-guide.pdf") or a full URL.
   */
  guidePdfUrl: "/snapback-guide.pdf",
};
